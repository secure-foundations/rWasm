[package]
name = "hello-wasi"
version = "0.1.0"
edition = "2021"

[lib]
crate-type = ["cdylib"]
path = "src/lib.rs"

[dependencies]

[profile.release]
strip = "debuginfo"
panic = "abort"

# compile with
# cargo build --release --target wasm32-wasi